{
  "name": "site",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": "24.x"
  },
  "scripts": {
    "dev": "mado dev",
    "build": "mado build",
    "typecheck": "mado typecheck",
    "test": "npm run typecheck && npm run test:browser",
    "test:browser": "node test/browser/site.browser.mjs",
    "verify": "npm run typecheck && npm run build",
    "release": "mado release",
    "verify:release": "node scripts/verify-release.mjs",
    "verify:edge": "node scripts/verify-edge.mjs",
    "preview": "mado preview",
    "preview:edge": "wrangler dev",
    "deploy": "node scripts/deploy-production.mjs",
    "new": "mado new"
  },
  "dependencies": {
    "@madojs/mado": "^0.14.2"
  },
  "devDependencies": {
    "@types/node": "^22.12.0",
    "playwright-core": "^1.61.1",
    "typescript": "^7.0.2",
    "vite": "^8.1.4",
    "wrangler": "^4.115.0"
  }
}
